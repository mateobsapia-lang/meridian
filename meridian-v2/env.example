RULES SPECIFICATION
Invariants:
1. A deal must belong to a valid authenticated user (ownerId == request.auth.uid).
2. Users can create pending deals.
3. Users can only update their own pending deals.
4. Only admins can update a deal to 'published'.
5. Only admins can list/read all pending deals.
6. Public (unauthenticated or unverified users) can read published deals.

Wait, the prompt says "Generate example payloads for each of the 8 pillars of hardened rules."

I am generating `DRAFT_firestore.rules` now.
